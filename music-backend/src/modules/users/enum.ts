export enum UserRole {
    USER = 1,
    ADMIN = 2,
  }

export enum UserGender{
  MALE = 1,
  FEMALE = 2
}

export enum UserFilter{
  EMAIL = "email",
  NAME = "name",
  GENDER = "gender",
  ID = "id"
}

export enum UserOrder{
  ASC = 'ASC',
  DESC = 'DESC',
  asc = 'asc',
  desc = 'desc',
}